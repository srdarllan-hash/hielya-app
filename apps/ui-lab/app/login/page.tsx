import { AuthRuntime } from '../../src/client/auth/AuthRuntime';

/** Temporary explicit login entry until the purchase/checkout flow exists. */
export default function LoginPage() { return <AuthRuntime />; }
