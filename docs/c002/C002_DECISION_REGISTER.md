# C-002 Pending Product Decisions

These decisions remain behind typed ports, policies or configuration and do not alter the implementation architecture:

1. destination chosen by the application layer after `LOCATION_CONFIRMED`;
2. route versus straight-line service-area method;
3. real reverse-geocoding provider;
4. real address-search provider;
5. guest retention duration and authenticated migration;
6. legal basis and final privacy copy;
7. operation timeouts, retry counts and backoff;
8. acceptable GPS accuracy threshold;
9. public-space and beach meeting-point policy;
10. quote expiration returned by the backend;
11. map preview usage;
12. store-closed navigation behaviour;
13. final illustrations and visual freeze reference.
