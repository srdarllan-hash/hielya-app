# HIELYA CHECKPOINT v8.1 — FORM_COMPONENTS_IMPLEMENTATION_CANDIDATE

2026-09-07 UTC. Previous v8.0; Issue #51, branch hielya/form-components-implementation, base main 9a6fb1451473652cfcb07b62cffe433e049871b4 (PR #49 merged). Policy, INDEX, CLAUDE, CONSTRAINTS and full requirements/FORM_COMPONENTS_SPECIFICATION read in this session; tokens 1.2.0 APPROVED_FROZEN confirmed. docs/specifications was a reference-path typo, corrected with owner acknowledgment.

Owner authorized Input/PhoneInput/OtpInput implementation only; no C-003/C-004 screens and NO MERGE. Additional visual decision: Reintentar uses existing Button secondary/md/automatic width, documented in spec §12. No other new visual values adopted.

Implementation: Input native control/shared feedback, controlled/uncontrolled values, ref and source-aware errors; PhoneInput mask/prefix, caret/selection, full-width normalization, paste rejection and blur/submit validation; OtpInput six real controls, single Tab entry, arrows/Home/End/Delete/Backspace, paste/autofill distribution, synchronous completion guard and explicit retry. Consumer owns transport/session/cooldown. Error results clear positions through controlled callback; network error preserves. No OTP logging/actions/analytics/storage; Storybook OTP stories use local state, no args/action instrumentation, synthetic fixtures only. autoFocus defaults false.

Added state stories for all three, unit tests, Chromium360/390 and WebKit390 Playwright tests with axe across states, keyboard/paste/geometry checks, and a dedicated additive form-components CI workflow. Existing Opaque Session workflow untouched. CSS visual values use existing tokens; token files unchanged. No new dependencies or backend/OpenAPI changes. Medium debt #50 unchanged.

Validation at checkpoint creation: 48 focused unit tests passed; TypeScript and focused ESLint passed; Storybook built successfully. Local Playwright browser download timed out; remote CI is required before requesting owner approval. Coverage report generated locally; final CI evidence belongs in PR metadata without rewriting historical checkpoints. No claim of physical iOS/Android SMS autofill or manual screen-reader certification; synthetic input-event coverage is distinct from real OS autofill.

Files added: packages/ui/src/components/{Input,PhoneInput,OtpInput}.tsx and corresponding stories; packages/ui/src/styles/forms.css; tests/unit/form-components.test.tsx; tests/form-components/forms.spec.ts; playwright.form-components.config.ts; .github/workflows/form-components-validation.yml; this checkpoint. Modified packages/ui/src/index.ts, styles.css, spec §12 and checkpoint INDEX. Historical checkpoints preserved; main unchanged. PR will remain unmerged for owner review after green CI. Next: remote validation, correct objective failures, report candidate SHA and checks, await merge authorization.
