# HIELYA CHECKPOINT v8.3 — PR52_MERGED / C003_C004_AUTHORIZED / CLARIFICATION_PENDING

2026-09-07 UTC. Previous repository checkpoint v8.1. A separately discovered Drive v8.2 explicitly classifies itself NON_CANONICAL_DRIVE_COMPATIBILITY_MIRROR; it is not imported or promoted. Version v8.3 avoids ambiguous reuse.

Owner explicitly approved PR #52 merge and authorized C-003 phone login and C-004 OTP implementation, with Issue → branch → implementation → tests → PR and separate future merge approval. PR #52 head 9af5a33d1b0d982875bec06b71db8cb2ba76ff51 was verified with all applicable checks successful (Design System 34145251347, forms 34145251374, contract 34145251353). Draft was marked ready and merged with expected-head guard. GitHub returned merged=true and main merge SHA 10410dded46dfc08e4247835d1ba957f48b3d953.

Issue #53 and branch hielya/c003-c004-auth-flow created from that exact merge. No screen, session policy, backend, token, workflow or production change implemented in this checkpoint. This file and the checkpoint INDEX entry are the only new branch changes. Historical checkpoints preserved. No secrets, credentials, OTP values, phone numbers or personal data included.

Authorized behavior: public catalog, login at purchase, Ahora no retained; canonical PhoneInput with default autofocus false and OtpInput with autoFocus=true. Application owns challengeId, server-derived cooldown and session; integrate existing POST /api/v1/auth/otp/request and /verify. Network verify failure preserves code and provides explicit retry. OTP_UNAVAILABLE uses approved message and cooldown-respecting new-code request. Issue #50 remains medium nonblocking debt; no backend recovery implementation. Real SMS, production and checkout implementation remain outside this scope.

Mandatory sources reviewed: CLAUDE.md, CONSTRAINTS, checkpoint policy/INDEX/v8.1, KNOWN_DEBT, INTEGRATIONS, opaque-session ADR, auth HTTP implementation and current catalog purchase-intent code. Historical CLAUDE statements that C003/C004 are not authorized are superseded by the owner's current explicit authorization; they must not be treated as current blockers. Existing session-consumption gaps remain factual.

Visual reference: Drive 1RodSJwcZPzcxeyff1C3B544EQOCdMw9e (PHONE_LOGIN_EMPTY option A) downloaded and visually inspected as REFERENCE_ONLY. It contains a cart-preservation promise; current HomeCatalogRuntime emits add-product/add-pack intentions and cartCount remains zero, so no existing cart persistence should be inferred. Drive search found Verificacao folder 1P3S94O0-ygreuRe6RjwVwIW_0qjR3pCb but returned no accessible children; C004 images remain unresolved, not presumed nonexistent.

Clarifications before implementation: (1) lifetime/storage of the sessionToken in the application after successful verify; existing ADR specifies server-side hash persistence but explicitly leaves client consumption unimplemented; (2) precise purchase entry trigger and post-success continuation while cart/checkout are absent; (3) exact C004 reference asset IDs or accessible folder. Do not silently invent these product/architecture policies. Screen composition dimensions and visuals must use approved tokens/components and any additional visual gap must be presented to the owner.

Next: obtain the missing definitions and references, implement application transport/state and screen compositions on Issue #53 branch, test accessibility/keyboard/network/cooldown/session boundaries, open implementation PR and require green CI before review. No authorization to merge that future PR. Main changed only by authorized PR52 merge; production unchanged.
