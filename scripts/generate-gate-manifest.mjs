import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';const root=process.cwd(),dir=path.join(root,'qa/gate');fs.mkdirSync(dir,{recursive:true});const sha=process.env.GITHUB_SHA||'LOCAL_NOT_OFFICIAL';const manifest={screenId:'C-005',branch:process.env.GITHUB_REF_NAME||'local',commitSha:sha,tokens:'1.1.0',components:'1.1.1',status:'QA_PASSED_CANDIDATE',generatedAt:new Date().toISOString(),checks:['lint','typecheck','unit','next-build','storybook-build','axe','visual-responsive','source-audit','manifest-audit']};fs.writeFileSync(path.join(dir,'C005_GATE_MANIFEST.json'),JSON.stringify(manifest,null,2));const files=[];for(const base of ['manifests','qa','screenshots']){const p=path.join(root,base);if(!fs.existsSync(p))continue;const walk=d=>{for(const e of fs.readdirSync(d,{withFileTypes:true})){const f=path.join(d,e.name);e.isDirectory()?walk(f):files.push(f)}};walk(p)}const sums=files.sort().map(f=>`${crypto.createHash('sha256').update(fs.readFileSync(f)).digest('hex')}  ${path.relative(root,f)}`);fs.writeFileSync(path.join(dir,'SHA256SUMS.txt'),sums.join('\n')+'\n');fs.writeFileSync(path.join(dir,'GATE_REPORT.md'),`# C-005 Gate 1A\n\n- Commit: ${sha}\n- Branch: ${manifest.branch}\n- Tokens: 1.1.0\n- Components: 1.1.1\n- Result: QA_PASSED_CANDIDATE\n\nFinal APPROVED_FROZEN requires review of this same commit's uploaded evidence.\n`);console.log(manifest);
