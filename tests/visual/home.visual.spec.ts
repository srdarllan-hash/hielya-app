import {test,expect} from '@playwright/test';import fs from 'node:fs';import path from 'node:path';const states=['ready','loading','closed','high-demand','error','empty-cart','alcohol-cutoff','out-of-area'];for(const state of states){test(`C-005 ${state} visual baseline`,async({page},testInfo)=>{await page.goto(`/?state=${state}`);await expect(page.locator('.hly-app-shell')).toHaveScreenshot(`C-005-${state}.png`,{animations:'disabled'});const dir=path.join('screenshots',testInfo.project.name);fs.mkdirSync(dir,{recursive:true});await page.screenshot({path:path.join(dir,`C-005_HOME_${state.toUpperCase()}.png`),fullPage:state==='ready'})})}
